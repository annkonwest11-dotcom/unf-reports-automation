# Ставки и KPI — единый справочник

Полная логика расчёта зарплаты. Источник данных — ответы Анны и прототип
`docs/БОТ_единый/bot_prototype.py` (там всё спроектировано), плюс боевой код
`sheets.py`.

> ⚠️ **Главное:** вся KPI-логика ниже **спроектирована, но НЕ внедрена в боевой
> бот.** Рабочие `bot.py` + `sheets.py` считают только базовые смены и подработки.
> Блоки KPI (поиск / Влада / РОП) живут только в прототипе. См. раздел 5.

**Обновлено:** 2026-07-06

---

## 1. Ставки за основную смену (₽)

Источник: `SHIFT_RATE` в прототипе.

| Группа | Будни | Выходной |
|---|---|---|
| Менеджер «поиск» | 2500 | — *(в выходные поиск не работает)* |
| Менеджер «сопровождение» | 2500 | 1800 |
| Влада | 3000 | 0 *(в выходной — только подработки)* |
| РОП (Анна) | 5000 | 5000 |

### ★ Стажёрская ставка (правило Анны от 01.09.2026, для ВСЕХ новых сотрудников)

**Первые 7 рабочих смен — 1 700 ₽, начиная с восьмой — полная ставка должности.**

Считается автоматически: в листе СМЕНЫ колонка D («Ставка») у стажёра — формула

```
=IF('НАСТРОЙКИ'!$B$37 + COUNT($E8:$AI8) < 'НАСТРОЙКИ'!$B$36; 'НАСТРОЙКИ'!$B$35; 'НАСТРОЙКИ'!$B$22)
```

где `B35` = 1 700 (стажёрская ставка), `B36` = 7 (сколько смен по ней), `B22` = 2 500
(полная ставка менеджера поиска), а `B37`/`B38` — сколько смен человек отработал
**в прошлых месяцах** (лист СМЕНЫ обнуляется при закрытии месяца, поэтому счётчик
переносится вручную; блок «СТАЖЁРСКАЯ СТАВКА СМЕНЫ» в НАСТРОЙКАХ, строки 33-38).
Бот читает эту ячейку перед записью смены и пишет в день ту ставку, которая
действует на момент записи — уже проставленные смены не пересчитываются.

### ★ План поиска для стажёра — 50% в первый месяц

Первый месяц работы план по обороту новых клиентов — **половина общего**
(105 000 вместо 210 000). Индивидуальные планы стажёров лежат в НАСТРОЙКАХ:
`C11` — София Хакимова, `C12` — Виолетта Караханова; со второго месяца ставим
туда полный план. Формула «% выполнения плана» в блоке стажёра ссылается именно
на его строку, а не на общий `C8`.

---

## 2. Ставки подработок (₽ за смену)

Источник: `SIDE_RATE` в прототипе.

| Тип | Ставка | Из чего |
|---|---|---|
| рест.1тел | 1800 | ресторан, 1 телефон |
| рест.2тел | 2100 | ресторан, 2 телефона (1800 + 300) |
| фирмы | 1800 | только фирмы |
| фирмы+1тел | 3600 | фирмы (1800) + 1 телефон (1800) |
| фирмы+2тел | 3900 | фирмы (1800) + 2 телефона (1800 + 300) |

**Влада — подработка фиксом за смену:** будни 2500, выходной 2000.

---

## 3. KPI менеджера «поиск»

**План:** `PLAN_POISK = 210 000 ₽/мес` на **каждого** менеджера поиска (оборот по
новым клиентам). Считается % выполнения плана, берётся **высший достигнутый
порог** из таблицы:

| % плана | Фикс-бонус | Доля от оплат |
|---|---|---|
| < 50% | 0 | 0 |
| ≥ 50% | 5 000 | 1% |
| ≥ 70% | 10 000 | 2% |
| ≥ 100% | 15 000 | 3% |
| ≥ 115% | 25 000 | 3% |
| ≥ 130% | 35 000 | 5% |
| ≥ 150% | 45 000 | 5% |
| ≥ 200% | 60 000 | 7% |

Функция: `kpi_poisk(plan_pct)` → `(фикс_бонус, доля_от_оплат)`.

---

## 4. KPI Влады (4 блока, ручной ввод за месяц)

Ответ Анны: вводятся **финальные суммы** по каждому блоку.

**БЛОК 1 — HR (бонусы за приём сотрудников)** — сумма достигнутого за месяц:
- Выполнен план по найму: 20 000 ₽
- Менеджер по продажам (7 раб. дней + РОП утвердил): +5 000 ₽
- Менеджер по продажам (месяц + РОП утвердил): +20 000 ₽
- Сотрудник на производство (месяц + руководитель утвердил): 10 000 ₽
- Водитель (месяц + логист утвердил): 20 000 ₽

**БЛОК 2 — SMM** (три отдельных поля: `smm_salary`, `smm_bonus`, `smm_clients`):
- Оклад за операционные задачи: 25 000 ₽/мес
- Премия за 90%+ плана контента: +5 000 ₽/мес
- Клиенты (за каждого): категория А — 7 000 ₽, В — 4 000 ₽, С — 2 000 ₽

**БЛОК 3 — Проекты:** премия за вовремя сданный проект (индивидуально, со
снижением за просрочку). Вводится финальное число.

**БЛОК 4 — Логистика:** бонус за администрирование, максимум 10 000 ₽/мес.
0 ошибок → 100%; каждая ошибка/пропуск/просрочка → −5% (−500 ₽).
Ответ Анны: вводится **финальное число** (ошибки вручную не считаем).

**Смены Влады:** будни — основные 3000 / подработки 2500; выходной — подработки 2000.

---

## 5. KPI РОП (Анна) — 3 блока бонусов + премия

**БЛОК 1 — оклад:** смена 5000 ₽ (будни). Подработки в выходные (фирмы/рестораны) — 1800 ₽/смена.

**БЛОКИ 2–4** — бонус от факта относительно плана (план вводится 1-го числа):

| Условие | Бонус |
|---|---|
| факт < 70% плана | 0 |
| 70% ≤ факт < 100% | факт × 0,7% |
| факт ≥ 100% | факт × 1% |
| факт ≥ 115% *(только Блок 2)* | +10 000 ₽ премия |

- **Блок 2** — новые продажи. План: `НАСТРОЙКИ!C13` (= 1 050 000). **С премией** за 115%+.
  Факт: KPI-таблица `1QuvmjSP…`, лист текущего периода (для июня — **«май-июнь»**),
  колонка **M «ИТОГ»** — сумма по всем строкам. (Колонки F–L — по менеджерам.)
- **Блок 3** — общий оборот. План: `НАСТРОЙКИ!C15` (= 10 000 000). **Без премии.**
  Факт: сумма ДАННЫЕ_Губарев + ДАННЫЕ_Перфильев (увеличение долга),
  **без конкурентов и закупок** (тип из листа СПРАВОЧНИК, колонка D `Тип`).
- **Блок 4** — поступления оплат. План: `НАСТРОЙКИ!C14` (= 8 500 000). **Без премии.**
  Факт: сумма из ДАННЫЕ (уменьшение долга), **только 5 менеджеров**
  (Дарья Вольнова, Ксения Наныкина, Лианна Багдасарян, Валерия Папоян, Прямой
  клиент). **Пока Анна вставляет вручную**, позже — интеграция ADesk → бот.

> Расчётный период берётся из `НАСТРОЙКИ!B4`. Планы поиска — `НАСТРОЙКИ!C8` (210 000).

Функции: `kpi_rop_block(plan, fact)`, премия — `kpi_rop_premium()`.

**Пример (из отчёта):** база 21 смена × 5000 = 105 000 + KPI (17 200 + 12 600 + 8 800) = **143 600 ₽**.

---

## 6. Что реально осталось уточнить

Уже НЕ вопрос (Анна ответила): структура ввода Влады = финальные суммы;
логистика = финальное число; премия 115% = только Блок 2.

Решено:
- ✅ Поиск в выходные не работает (ставки выходного нет).
- ✅ Оборот (Блок 3): ДАННЫЕ_Губарев+Перфильев, кол. C, без конкурентов/закупок.
- ✅ Поступления (Блок 4): пока вручную, потом ADesk.
- ✅ KPI-входы хранить в листе `KPI_МЕСЯЦ`.

Решено (дополнительно):
- ✅ Премия 115% — только Блок 2 (новые продажи).
- ✅ Новые продажи: KPI-таблица, лист «май-июнь», колонка M «ИТОГ».
- ✅ Конкуренты/закупки: тип в листе СПРАВОЧНИК (кол. D `Тип`).

- ✅ Колонки ДАННЫЕ_* определены (тождество B+C−D=E, 473/473 строк):
  B = долг начало, **C = увеличение = оборот**, **D = уменьшение = оплаты**, E = конец.

Открыто (только ручные данные, не структура):
- [ ] Список сотрудников на июнь и кто «поиск» / кто «сопровождение».
- [ ] Ручные входы за июнь для Влады (HR/SMM/Проекты/Логистика).
- [ ] Факт поступлений РОП (Блок 4): вручную или считать из ДАННЫЕ кол. D по 5 менеджерам.

---

## 7. Статус внедрения (важно)

| Что | Спроектировано | В боевом коде (`sheets.py`) |
|---|---|---|
| Базовые смены + подработки | ✅ | ✅ (частично, свои ставки) |
| KPI «поиск» (таблица порогов) | ✅ прототип | ❌ нет |
| KPI Влады (4 блока) | ✅ прототип | ❌ нет |
| KPI РОП (3 блока + премия) | ✅ прототип | ❌ нет |
| Хранение KPI-входов (лист KPI_МЕСЯЦ) | предложено | ❌ нет |
| Чтение фактов из 1С/листов | TODO | ❌ нет |

**Вывод:** чтобы «зарплатная система» считала ЗП полностью, KPI-слой из
прототипа нужно перенести в боевой `sheets.py`/`bot.py` и подключить хранение
входов + источники фактов.
