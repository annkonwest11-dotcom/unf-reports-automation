# ✅ Текущее состояние системы

**Дата:** 2026-06-25  
**Статус:** 🟡 Работает, но нужен рефакторинг

---

## ✅ Что работает

### 1. Telegram Bot для смен (`bot.py`)
```
✅ Получает отчёты о смене в групповом чате
✅ Парсит текст (используя parser.py)
✅ Сохраняет в Google Sheets лист СМЕНЫ
✅ Регистрирует новых пользователей (/start)
✅ Проверяет аномалии (низкие звонки, ранние отчёты, неправильная дата)
✅ Отправляет алерты Анне в личку
✅ Автоматически пишет смену Анне (РОП) в 11:00
✅ Проверяет кто не сдал отчёт (в 21:00)
✅ Синхронизирует 1С по расписанию (в 7:00)
```

### 2. Google Sheets (`sheets.py`)
```
✅ Читает/пишет данные в таблицу
✅ Заполняет лист СМЕНЫ
✅ Архивирует месяц (копирует листы, очищает рабочие)
✅ Вычисляет итоговые суммы
✅ Управляет доступом (only via API)
```

### 3. Синхронизация 1С (`sync_42clouds_v2.py`)
```
✅ Загружает данные из OData (Перфильев и Губарев)
✅ Распарсивает дебиторку
✅ Распарсивает взаиморасчёты
✅ Сохраняет в Google Sheets
```

### 4. Команды для Анны
```
✅ /start          — регистрация
✅ /sync_1c        — синхронизация 1С
✅ /archive        — архивирование месяца
✅ смена ДАТА      — получить отчёт за день (из Google Sheets)
✅ /today          — отчёт за сегодня (из 1С OData)
✅ /range ДД ДД   — отчёт за период (из 1С OData)
```

---

## ❌ Что НЕ работает или требует внимания

### 1. **Дублирование кода** 🔴
```
❌ telegram_reports.py и Code.gs делают одно и то же
❌ OData парсер в двух местах (Python + GAS)
❌ Код сложно поддерживать
```

### 2. **Команды отчётов в telegram_reports.py**
```
❌ /reports_perfilev, /reports_gubarev работают, но:
   - OData запросы не оптимизированы
   - Нет обработки ошибок
   - Нет тайм-аутов
   - Нет кэширования
```

### 3. **Google Apps Script (Code.gs)**
```
⚠️  Параллельно работает с Python ботом
⚠️  Может быть источником конфликтов
⚠️  Имеет свои OData парсеры
```

### 4. **Архитектура запутана**
```
❌ Весь код в bot.py (1000+ строк)
❌ Нет разделения на слои (routing → handlers → clients)
❌ Трудно добавить новый функционал
❌ Трудно писать тесты
```

---

## 📊 Тесты

```
❌ Нет юнит-тестов
❌ Нет интеграционных тестов
❌ Ручное тестирование в боевом окружении
```

---

## 🐛 Известные проблемы

| Проблема | Статус | Когда заметили |
|----------|--------|----------------|
| OData timeout при больших данных | 🟡 Работает, но медленно | 2026-06-23 |
| Дублирование в telegram_reports.py | 🔴 Нужен рефакторинг | 2026-06-20 |
| Возможны конфликты с GAS | 🟡 Пока не было | неизвестно |
| Нет логирования ошибок | 🟡 Логи только в консоль | всегда |

---

## 📈 Производительность

| Операция | Время | Статус |
|----------|-------|--------|
| Парсинг смены | < 100ms | ✅ Быстро |
| Запись в Google Sheets | ~500ms | ✅ Нормально |
| Загрузка OData (продажи) | ~2s | ⚠️ Медленно |
| Загрузка OData (дебиторка) | ~5s | ⚠️ Очень медленно |
| Синхронизация 1С полностью | ~30s | ⚠️ Очень медленно |

---

## 🔄 Потоки данных

### Текущие потоки
```
Telegram → bot.py → Google Sheets
              ↓
          telegram_reports.py → Telegram (отчёты)
              ↓
          Code.gs → Telegram (webhook + отчёты)
              ↓
          sync_42clouds_v2.py → Google Sheets
```

### Проблема
```
Нет ясной иерархии. Несколько компонентов делают одно.
```

---

## 📁 Текущая структура файлов

```
claude-test/
├── bot.py                    ← 850 строк (парсинг + обработчики + логирование)
├── parser.py                 ← OK (парсинг смен)
├── sheets.py                 ← OK (работа с Sheets)
├── sync_42clouds_v2.py       ← OK (синхронизация)
├── telegram_reports.py       ← ❌ ДУБЛИРУЕТ Code.gs (600 строк)
├── Code.gs                   ← ❌ ДУБЛИРУЕТ telegram_reports.py (1000 строк)
├── .env                      ← Конфигурация (хорошо)
├── employees.json            ← Регистрация пользователей (OK)
├── last_update_id.txt        ← Offset для восстановления (OK)
└── docs/                     ← Документация (устаревшая)
```

---

## 🎯 Что нужно сделать

### Фаза 1: Рефакторинг архитектуры (URGENT)
```
[ ] Создать папку handlers/
    [ ] shift_handler.py
    [ ] report_handler.py
    [ ] admin_handler.py
    
[ ] Создать папку clients/
    [ ] odata_client.py (единый OData парсер)
    [ ] telegram_client.py (отправка)
    
[ ] Переписать bot.py (оставить только routing)
[ ] Удалить дублирование (telegram_reports.py)
```

### Фаза 2: Улучшение кода
```
[ ] Добавить обработку ошибок
[ ] Добавить timeout для OData
[ ] Добавить кэширование
[ ] Добавить юнит-тесты
```

### Фаза 3: Миграция (опционально)
```
[ ] Переписать Code.gs на Python
[ ] Убрать Google Apps Script зависимость
[ ] Запустить всё на Python
```

---

## 💾 Данные

### Google Sheets
```
Основная таблица: /Users/anna/claude-test/.env → SPREADSHEET_ID
Листы:
  - СМЕНЫ (основные данные)
  - СВОДНАЯ_ЗП (расчёты)
  - ИНТЕГРАЦИЯ (дебиторка из 1С)
  - ВЗАИМОРАСЧЁТЫ (из 1С)
  - DASHBOARD (сводка)
  - LOG (логи)
  - QUEUE (очередь команд)
  - RAW_perfilev (сырые данные)
  - RAW_gubarev (сырые данные)
```

### Локальные файлы
```
.env                    ← Secrets (BOT_TOKEN, SPREADSHEET_ID, etc)
employees.json          ← user_id → name mapping
last_update_id.txt      ← Offset для восстановления обновлений
credentials.json        ← Google API credentials (не коммитится)
```

---

## 🔐 Конфигурация

### Переменные окружения (в .env)
```
BOT_TOKEN               ← Telegram Bot Token
SPREADSHEET_ID          ← Google Sheet ID
ANNA_CHAT_ID            ← Telegram ID Анны
GROUP_CHAT_ID           ← ID рабочей группы
CREDENTIALS_PATH        ← Путь к credentials.json (для Google API)
```

### OData credentials (в Code.gs Properties)
```
ODATA_USER              = "api_bot"
ODATA_PASS              = "slavaperfilev1414"
```

### Базы 1С (константы)
```
Перфильев: base_id=152757
Губарев: base_id=64904
OData endpoint: https://base.42clouds.com/unf/{base_id}/odata/standard.odata/
```

---

## 📞 Контакты внешних систем

| Система | URL | Auth | Статус |
|---------|-----|------|--------|
| Telegram API | api.telegram.org | BOT_TOKEN | ✅ Работает |
| Google Sheets | sheets.googleapis.com | credentials.json | ✅ Работает |
| OData (Перфильев) | base.42clouds.com/unf/152757 | Basic Auth | ✅ Работает |
| OData (Губарев) | base.42clouds.com/unf/64904 | Basic Auth | ✅ Работает |

---

## 📝 История обновлений

```
2026-06-18  Добавлена защита от повторной обработки Telegram апдейтов
2026-06-17  Обновлено получение данных из 1С API
2026-06-xx  Создана UNF Reports Automation (готовая система)
```
