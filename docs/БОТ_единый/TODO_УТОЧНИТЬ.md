# ⚠️ СПИСОК УТОЧНЕНИЙ ДЛЯ ИНТЕГРАЦИИ KPI

Все `# TODO:уточнить` отмечены в `bot_prototype.py` — вот полный список с номерами строк.

---

## 1️⃣ ВЛАДА: Как вводить кол-во ошибок логистики? (строка 462)

**Текущий код:**
```python
# TODO:уточнить: как вводить "logistics_errors" (кол-во ошибок)? пока максимум 10000.
```

**Вариант A:** Вводить финальное число (10k или менее)
```python
"logistics": 9500  # человек вычел 500 за ошибку
```

**Вариант B:** Вводить кол-во ошибок, бот вычислит
```python
"logistics_errors": 1  # бот считает: 10000 - 500 = 9500
```

**УТОЧНИТЬ: Какой вариант выбираем?**

---

## 2️⃣ ВЛАДА: Структура SMM-данных (строка 512–518)

**Текущий код:**
```python
vlada_smm = float(inp.get("smm_salary", 0) or 0) + 
            float(inp.get("smm_bonus", 0) or 0) + 
            float(inp.get("smm_clients", 0) or 0)
```

**Вопрос:** Эти три поля `smm_salary`, `smm_bonus`, `smm_clients` — вводить ли отдельно?

**Вариант A (текущий в коде):** Да, три поля
```python
kpi_inputs["Влада"] = {
    "smm_salary": 25000,   # оклад
    "smm_bonus": 5000,     # премия 90% контента
    "smm_clients": 7000,   # сумма за клиентов (А 7k, В 4k, С 2k)
}
```

**Вариант B:** Одно поле SMM (общая сумма)
```python
kpi_inputs["Влада"] = {
    "smm_total": 37000,    # всё вместе
}
```

**УТОЧНИТЬ: Какой вариант выбираем?**

---

## 3️⃣ ВЛАДА: ПРОЕКТЫ — как вводить премии? (строка 520)

**Текущий код:**
```python
"projects": 0,        # TODO:уточнить: сумма премий или с вычетом просрочки?
```

**Вопрос:** Как Анна будет вводить эту цифру?

**Вариант A:** Вводит финальное число (уже учтена просрочка)
```python
"projects": 15000  # 20k базовая премия - 5k штраф за 2 дня просрочки = 15k
```

**Вариант B:** Вводит детали, бот считает
```python
"projects": {
    "completed": 20000,     # базовая премия
    "late_days": 2,         # дней просрочки
    "late_penalty": 2500,   # штраф за день
}
# бот считает: 20000 - (2 × 2500) = 15000
```

**УТОЧНИТЬ: Какой вариант выбираем?**

---

## 4️⃣ РОП АННА: Откуда брать ФАКТЫ? (строка 595)

**Текущий код:**
```python
# KPI РОП (3 блока бонусов + премия за новые продажи)  # TODO:уточнить точные имена входов
```

**Проблема:** Планы вводятся вручную, но факты откуда?

**Вопрос 4a:** ФАКТ НОВЫХ ПРОДАЖ
- Где вообще считаются "новые клиенты"? (в Google Sheets лист СПРАВОЧНИК?)
- Автоматически из 1С или вручную вводит Анна?

**Вопрос 4b:** ФАКТ ОБОРОТА
- Это просто все продажи РОП из 1С за месяц?
- Есть ли фильтр "только новые клиенты" или все клиенты?

**Вопрос 4c:** ФАКТ ПОСТУПЛЕНИЙ
- Это лист ВЗАИМОРАСЧЁТЫ (уменьшение долга РОП)?
- Или из 1С (AccumulationRegister какой-то)?

**УТОЧНИТЬ: Для каждого факта выбрать источник**

---

## 5️⃣ ОБЩЕЕ: Где хранить KPI-входы? (строка 619)

**Текущий код:**
```python
# TODO:уточнить — откуда брать KPI данные (из таблицы, из отдельного листа-справочника?))
```

**Вариант A:** Лист "KPI_МЕСЯЦ" в Google Sheets
```
Месяц  | Сотрудник | Поле           | Значение
2026-6 | Влада     | hr             | 20000
2026-6 | Влада     | smm_salary     | 25000
2026-6 | Анна      | plan_new_sales | 600000
```

**Вариант B:** JSON в листе НАСТРОЙКИ
```json
{"kpi_2026_6": {
  "Влада": {"hr": 20000, "smm_salary": 25000, ...},
  "Анна": {"plan_new_sales": 600000, ...}
}}
```

**Вариант C:** Где-то ещё (указать)

**Дополнительно:**
- Кто вводит: Анна, Вячеслав, кто ещё?
- Когда: в начале месяца или по мере?

**УТОЧНИТЬ: Выбрать место хранения + кто вводит**

---

## 6️⃣ РОП: Есть ли ПРЕМИЯ за 115%+ в БЛОКАХ 3 и 4? (строка 595)

**Текущий код:**
```python
r.rop_new_sales = kpi_rop_block(plan_new, fact_new) + kpi_rop_premium(plan_new, fact_new)  # премия есть
r.rop_turnover = kpi_rop_block(plan_turn, fact_turn)  # премии нет
r.rop_payments = kpi_rop_block(plan_pay, fact_pay)    # премии нет
```

**Вопрос:** В ТЗ премия 10000 ₽ есть ТОЛЬКО в Блоке 2 (новые продажи). А в Блоках 3 и 4?

**Вариант A (текущий):** Премия только в Блоке 2
```
Блок 2: 115%+ → +10000 ₽
Блок 3: нет премии
Блок 4: нет премии
```

**Вариант B:** Премия во всех трёх блоках
```
Блок 2: 115%+ → +10000 ₽
Блок 3: 115%+ → +10000 ₽
Блок 4: 115%+ → +10000 ₽
```

**УТОЧНИТЬ: В каких блоках есть премия за 115%+?**

---

## 7️⃣ ЛИСТА СВОДНАЯ_ЗП: Какие колонки для KPI? (строки 565–575)

**Текущий код:**
```python
# КПИ по группам
if r.group == "poisk":
    row_data.extend([ f"{r.plan_pct}%", round(r.kpi_fix), round(r.kpi_pay) ])
elif r.group == "vlada":
    row_data.extend([ round(r.vlada_hr), round(r.vlada_smm), round(r.vlada_projects), round(r.vlada_logistics) ])
elif r.group == "rop":
    row_data.extend([ round(r.rop_new_sales), round(r.rop_turnover), round(r.rop_payments) ])
```

**Вопрос:** Какие колонки должны быть в реальном листе СВОДНАЯ_ЗП?

**Вариант A:** Все детали отдельно
```
Сотрудник | Группа | База | Подр | % план | HR | SMM | Проекты | Логистика | Новые | Оборот | Платежи | ИТОГО
```

**Вариант B:** Только сумма KPI
```
Сотрудник | Группа | База | Подр | KPI_СУММА | ИТОГО
```

**Дополнительно:**
- Есть ли ограничение по кол-ву колонок?
- Нужна ли история (как считалось) или только итог?

**УТОЧНИТЬ: Согласовать структуру листа СВОДНАЯ_ЗП**

---

## 📋 ИТОГОВЫЙ ЧЕКЛИСТ ДЛЯ АННЫ

Распечатай и заполни:

- [ ] 1️⃣ Логистика ошибок: финальное число или кол-во?
- [ ] 2️⃣ SMM: три поля или одно?
- [ ] 3️⃣ Проекты: финальная сумма или с расчётом?
- [ ] 4️⃣ РОП факты: откуда брать (новые, оборот, платежи)?
- [ ] 5️⃣ KPI справочник: где хранить, кто вводит?
- [ ] 6️⃣ РОП премия: в блоках 2, 3, 4 или только в 2?
- [ ] 7️⃣ Лист ЗП: какие колонки для KPI?

**Отправь ответы, и я обновлю код сразу.**

---

**Код готов к использованию, как только получу ответы на эти 7 вопросов.**
